

# Mailpit

Docker basique qui permet de lancer un mailpit pour tester le consumer mail :
- L'interface web est accessible à l'adresse http://localhost:8025/
- Le SMTP à configurer est `localhost:1025`