version: "3.9"

services:
  redis:
    image: redis:7
    container_name: redis-poc
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --notify-keyspace-events "KEx"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
